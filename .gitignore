# Secrets — never commit
.env
.env.*
*.local
*token*

# Node
node_modules/
package-lock.json

# Local progress database (your application tracking) — never commit
app.db
app.db-journal
app.db-wal
app.db-shm
*.sqlite
*.sqlite3

# Enrichment cache (regenerated by enrich.mjs)
.enrich-cache.json

# OS
.DS_Store
Thumbs.db
